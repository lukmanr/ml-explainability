from setuptools import setup

setup(name='anchor_cmle',
      version='0.1',
      description='Use anchor explainability on models hosted in CMLE',
      packages=['anchor_cmle'],
      zip_safe=False)